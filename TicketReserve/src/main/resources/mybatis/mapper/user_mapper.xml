<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.office.ticketreserve.user.IUserDaoForMybatis">

	<delete id="deleteUser" 
			parameterType="Integer">
		DELETE FROM 
			TBL_USER 
		WHERE 
			U_NO = #{u_no}
	</delete>
	<select id="selectUser" parameterType="int">
		SELECT * FROM 
			TBL_USER 
		WHERE
			U_NO = #{u_no}
	</select>
	<select id="getMyTicketinfo" parameterType="String" resultType="com.office.ticketreserve.reservation.ReservationDto">
		SELECT * FROM
			TBL_RESERVE
		WHERE
			U_ID = #{u_id}
		ORDER BY
			R_REG_DATE	DESC
			
	</select>
	<select id="getPerfomanceId"  parameterType="String" resultType="java.lang.String">
		SELECT P_ID
			FROM TBL_TICKET
		WHERE
			T_NO = #{t_no}
	</select>
	<select id="getPerfomaceInfo" parameterType="String" resultType="com.office.ticketreserve.productpage.PerfomanceDto">
		SELECT * FROM 
			TBL_PERFOMANCE
		WHERE
			P_ID = #{p_id}
	</select>
		<!-- <select id="dofindIdFromDB"  parameterType="string" 
	        resultType="string">
	        
	    SELECT u_id
	 	FROM TBL_USER
	    WHERE U_NAME = #{u_name} 
	    AND 
	    U_MAIL = #{u_mail}
	    
	    
	</select> -->
	<!-- <select id="getPerfomanceId" parameterType="java.util.List" resultType="java.lang.String">
    SELECT P_ID
    FROM TBL_TICKET
    WHERE T_NO IN
    <foreach item="t_no" index="index" collection="list" open="(" separator="," close=")">
        #{t_no}
    </foreach> 
    
</select>-->
</mapper>